// Temporary stub – will be replaced with real Gmail API integration later
exports.sendVerificationEmail = async (to, verificationUrl) => {
  console.log(`[Stub] Sending verification email to ${to}: ${verificationUrl}`);
  return Promise.resolve();
};